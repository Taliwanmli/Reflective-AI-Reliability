[project]
name = "reflective-ai-reliability"
version = "0.1.0"
description = "Offline validation tools for a draft reflective-AI reliability study"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Shiwen Tian" }]
dependencies = []

[tool.unittest]
start-directory = "tests"
pattern = "test_*.py"
